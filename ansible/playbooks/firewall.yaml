---

- name: Setup firewall UFW
  hosts: all
  tasks:

  - name: Reject everything and enable UFW
    community.general.ufw:
      state: enabled
      policy: allow

  # ufw supports connection rate limiting, which is useful for protecting
  # against brute-force login attacks. ufw will deny connections if an IP
  # address has attempted to initiate 6 or more connections in the last
  # 30 seconds. See  http://www.debian-administration.org/articles/187
  # for details. Typical usage is:
  - community.general.ufw:
      rule: limit
      port: ssh
      proto: tcp

  # Allow OpenSSH. (Note that as ufw manages its own state, simply removing
  # a rule=allow task can leave those ports exposed. Either use delete=true
  # or a separate state=reset task)
  - community.general.ufw:
      rule: allow
      name: OpenSSH

  - name: Allow all access to tcp port 80 aka http
    community.general.ufw:
      rule: allow
      port: '80'
      proto: tcp

  - name: Allow all access to tcp port 443 aka https
    community.general.ufw:
      rule: allow
      port: '443'
      proto: tcp
